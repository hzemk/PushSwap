#include <libstd.h>
#include <stdio.h>
#include <unistd.h>

int	main(int argc, char *s[])
{
	t_stack	*a;
	t_stack	*b;

	if (argc >= 2)
	{
		a->bench->disorder = disoreder(a);
		select_algo(a, b, s);
	}
}
